name: Deployment

on:
  push:
    branches:
    - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
    - uses: actions/checkout@v4
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    - name: Install Dependencies
      run: npm install
    - name: Build Application and Run Unit Tests
      run: CI=false npm run build
    - name: Deploy to S3
      uses: jakejarvis/s3-sync-action@main
      with:
        args: --acl public-read --delete --exclude ".git/*" --exclude "README.md"
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
        AWS_REGION: ${{ secrets.AWS_REGION }}  # optional: or use AWS_REGION: 'ap-south-1' # for India
        SOURCE_DIR: build/                     # optional: defaults to entire repository, or use SOURCE_DIR: ${{ secrets.SOURCE }}
